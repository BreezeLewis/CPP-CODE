#define _CRT_SECURE_NO_WARNINGS
//#include <stdio.h>
//long long a[51], x, y, z;
//int main()
//{
//	scanf("%lld%lld%lld", &x, &y, &z);
//	for (long long i = 1; i <= x + 2; i++)
//		a[i] = 1;
//	for (long long i = x + 3; i <= z + 1; i++)
//		a[i] = a[i - 1] + y * a[i - x - 2];
//	printf("%lld\n", a[z + 1]);
//	return 0;
//}