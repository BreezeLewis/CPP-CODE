#include <iostream>
#include <vector>

using namespace std;

int main()
{
	/*int n;
	cin >> n;
	vector<bool> v(n, true);

	for (int i = 2; i <= n; i++)
	{
		for (int j = 0; j < v.size(); j++)
		{
			if ((j + 1) % i == 0)
				v[j] = !v[j];
		}
	}

	for (int i = 0; i < v.size(); i++)
	{
		if (v[i] == true)
			cout << i + 1 << " ";
	}*/



	return 0;
}