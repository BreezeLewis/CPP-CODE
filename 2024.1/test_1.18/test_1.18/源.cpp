#include <iostream>
#include <algorithm>
#include <cmath>
#include <vector>

using namespace std;

int main()
{
	//double a, b, c;

	//cin >> a >> b >> c;

	//double p = (a + b + c) / 2;

	//double s = sqrt(p * (p - a) * (p - b) * (p - c));

	///*cout << s << endl;*/

	//printf("%.3lf\n", s);

	//int a;
	//cin >> a;
	////cout << (a % 2 == 0 ? "yes" : "");

	//cout << ((a > 1 && a < 100) ? "yes" : "");

	/*int a[3] = {};

	cin >> a[0] >> a[1] >> a[2];

	sort(a, a + 3, greater<int>());

	for (int i = 0; i < 3; i++) 
	{
		cout << a[i] << " ";
	}*/

	/*int t;
	cin >> t;

	cout << ((t >= 25 && t <= 30) ? "ok!" : "no!");*/




	return 0;
}